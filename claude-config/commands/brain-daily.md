---
description: Roll up today's coding activity into the daily note. Scans commits across active project repos, vault edits, and inbox additions. Idempotent — safe to run multiple times per day.
---

Invoke the `daily-rollup` skill at
`/home/user/Obsidian/SecondBrain/90-meta/skills/daily-rollup/SKILL.md`.

Run the full workflow including the light-mode `graph-health` pass and
the `auto-link` post-step. Append-only merge — do not delete user-written
content in today's daily.
