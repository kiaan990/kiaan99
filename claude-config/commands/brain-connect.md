---
description: Find prior work on a topic — cluster-style search across retros and patterns. Goes beyond keyword grep; reports clusters and proposes new patterns when a dense cluster has no pattern file.
argument-hint: <topic or question>
---

Invoke the `connect-dots` skill at
`/home/user/Obsidian/SecondBrain/90-meta/skills/connect-dots/SKILL.md`
with seed: $ARGUMENTS

Build clusters by tag overlap + wikilink neighborhood + textual
similarity. For any cluster of size ≥3 with no pattern file, propose
scaffolding one and ask the user.
