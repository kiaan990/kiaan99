---
description: Audit the Second Brain graph — orphan notes, broken wikilinks, stale active projects, weak clusters, frontmatter violations. Use full mode for a weekly audit.
argument-hint: [light|full]   (default: full)
---

Invoke the `graph-health` skill at
`/home/user/Obsidian/SecondBrain/90-meta/skills/graph-health/SKILL.md`
in mode: $ARGUMENTS (default: full)

Always propose repairs, never auto-fix. Section the report by category.
